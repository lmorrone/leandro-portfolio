---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<section class="mx-auto mb-8 mt-0 max-w-[700px]">
  {title && <h2 class="mb-3 text-xl font-bold">{title}</h2>}
  <slot />
</section>
