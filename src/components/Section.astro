---
interface Props {
  title?: string,
}

const { title } = Astro.props;
---

<section class="max-w-[700px] mt-0 mx-auto mb-8">
  {title && <h2 class="mb-3 font-bold text-xl">{title}</h2>}
  <slot />
</section>
